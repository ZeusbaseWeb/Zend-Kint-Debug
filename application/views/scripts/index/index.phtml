<p>
    An example of Kint library usage:<br />
    <b>Kint::dump($variable1, $variable2, ...);</b><br />
    or...<br />
    <b>d($variable1, $variable2, ...);</b> // <- is an alias for Kint::dump()<br />
    <b>dd($variable1, $variable2, ...);</b> // <- alias for Kint::dump() which stops script execution (basic die())
</p>